[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[scripts]
main = "env TZ=UTC python run.py"
tests = "coverage run -m pytest"
check = "bash -c 'black . && mypy . && mypy --install-types'"

[packages]
uvloop = {version = "*", markers = "sys_platform == 'linux'"}
"discord.py" = "*"
redis = "*"
typeddict-validator = "*"

[dev-packages]
mypy = "*"
black = "*"
coverage = "*"
pytest = "*"

[requires]
python_version = "3.11"
